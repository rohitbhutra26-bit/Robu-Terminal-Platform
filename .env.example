# Copy to .env.local and fill in. All optional for basic use.

# Where the Valuation data server runs (default is fine for local).
DATA_SERVER_URL=http://localhost:8000

# Optional — enables Valuation's AI overview / peer suggestions (Google Gemini).
# Without it, those AI bits just show a graceful fallback.
GEMINI_API_KEY=
